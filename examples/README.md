# examples/

Hello-clip brief notes that users can copy + tweak instead of starting from a blank brief.

T3 fills this with 2-3 representative `*.note` files (different styles, durations, aspects). For now it's a placeholder.

> **Purity reminder**: examples are user-facing; `bun run check:purity` scans them. Do not use business concepts (`episode|shot|series|pipeline`) in sample briefs. <!-- purity-allow: documenting the check -->
